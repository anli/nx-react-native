import React, { ReactNode } from 'react'
import { SafeAreaView, Text } from 'react-native'

const App = (): ReactNode => {
  return (
    <SafeAreaView>
      <Text>Welcome</Text>
    </SafeAreaView>
  )
}

export default App
